= content_for :title_header do
  | BookBuilding | User Dashboard
end

.ui.vertical.stripe.segment
  .ui.middle.aligned.stackable.grid.container
    .row
      .column 
        .ui.fluid.card
          .ui.segment
            h1.ui.header
              | Name: &nbsp;
              = current_user.name
            .ui.horizontal.segments
              .ui.segment
                = link_to user_profile_path, class: "ui massive primary fluid button" do
                  i.user.icon
                  | Profile
              .ui.segment
                = link_to users_favorites_path, class: "ui massive green fluid button" do
                  i.star.icon
                  | Favorite
              .ui.segment
                = link_to users_bookings_path, class: "ui massive teal fluid button" do
                  i.star.icon
                  | Booked Rooms